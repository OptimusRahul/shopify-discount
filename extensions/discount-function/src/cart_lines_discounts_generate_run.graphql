query CartInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          product {
            id
          }
        }
      }
    }
  }
  shop {
    metafield(namespace: "volume_discount", key: "rules") {
      value
    }
  }
}
